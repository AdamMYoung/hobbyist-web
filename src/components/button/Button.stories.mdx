import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Button from './';

<Meta title="Components/Atom/Button" component={Button} />

# Button

Button with variants

export const Template = (args) => <Button {...args}>My Button</Button>;

### Default

<Canvas>
    <Story
        name="Primary"
        args={{
            variant: 'primary',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable for={Button} />
