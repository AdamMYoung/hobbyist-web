import styled from 'styled-components';
import tw from 'twin.macro';

export const Input = styled.input`
    ${tw`focus:ring focus:outline-none border transition rounded-sm py-2 px-3 border-gray-300`}
`;
